[package]
name = "rust_commander"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatui = "0.26.1"   # Gestisce l'interfaccia grafica (i pannelli, i bordi, le liste)
crossterm = "0.27.0" # Gestisce il terminale Windows e i tasti (come la 'q' per uscire)
